Setup environment (to be removed )

Server:

```
mkdir server && cd server && npm init -y && \
npm install typescript ws express && \
npm install -D nodemon ts-node tsc-watch @types/node @types/ws @types/express && \
npm install @fluvio/client -S 
```
